# mjneuroart

Profile repo. `README.md` is the profile page; `tools/build_banner.py` regenerates the banners (run nightly by `.github/workflows/uplink.yml`); `docs/` is the GitHub Pages terminal site. Fonts under `assets/fonts/` are SIL OFL, licenses included. The face image is my own Midjourney creation.
